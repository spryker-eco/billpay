<div class="small-12 medium-9 large-6">
    <div class="small-12 columns">
        <label>
            {{ form_widget(form.billpayInvoice.date_of_birth) }}
        </label>
        {{ form_errors(form.billpayInvoice.date_of_birth) }}
    </div>
</div>

<div class="bpy bpy-checkout-container" bpy-pm="invoice">
</div>

<!-- Initialization & Configuration -->
<script type="application/javascript">
    var bpyReq = {};
    var appPath = '//widgetcdn.billpay.de/checkout/1.x.x/';

    (function(win, doc, appPath, objectName) {
        bpyReq = {
            "deps": ['main'],
            "baseUrl": appPath,
            "skipDataMain": true,
            "callback": function() {}
        };
        win['BillPayCheckout'] = objectName;
        win[objectName] = win[objectName] || function() {
                (win[objectName].queue = win[objectName].queue || []).push(arguments)
            };
        var requireJs = doc.createElement('script');
        requireJs.src = appPath + 'require.js';
        doc.getElementsByTagName('head')[0].appendChild(requireJs);
    })(window, document, appPath, 'billpayCheckout');
</script>

{% include '@Billpay/layout/partials/widget-data.twig' %}

<script src="/assets/default/js/spryker-yves-billpay-main.js"></script>